```clojure
;; Describe the behavior of the following procedure using our model of evaluation
;;		(defn a-plus-abs-b [a b]
;;  		((if (> b 0) + -) a b))

The if statement becomes either a -  or a + the following operand as the if statement is evaluated

```
