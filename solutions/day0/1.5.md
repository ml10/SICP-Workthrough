```clojure
;;(defn p [] (p))

;;(defn test [x y]
;;  (if (= x 0)
;;      0
;;      y))

;;evaluate (test 0 (p))

;;applicative-order evaluation
stack overflow as p will keep expanding on itself as a possible solution

;;normal-order evaluation
0

```